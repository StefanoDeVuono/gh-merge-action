import test from 'ava'


test('foo', t => t.pass())

test('bar', t => t.pass())

test('ok', t => t.pass())

test('other', t => t.pass())

test('the other thing too', t => t.pass())

// test('this one fails', t => t.fail())
